{
  "tabs": [
    {
      "id": "display",
      "label": "Display",
      "fields": [
        { "key": "show_billing_period", "label": "Show Billing Period", "component": "checkbox", "description": "Display billing period on plan cards" },
        { "key": "show_plan_features", "label": "Show Plan Features", "component": "checkbox", "description": "Display feature list on plan cards" }
      ]
    }
  ]
}
